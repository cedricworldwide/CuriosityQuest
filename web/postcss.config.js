/**
 * PostCSS configuration file. We use PostCSS to apply Tailwind CSS
 * and Autoprefixer to our styles. Vite automatically picks up
 * this config when building the project.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};